const initialState = {
    panel: {
        open: false
    }
}

const rules = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default rules