const initialState = {
    byId: {},
    allIds: []
}

const players = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default players